//- demo: true
mixin textBox(args)
	include cssmodule
	+module('div', css.module, args)
		- let args = item

		div(class=css.icon)
			i(class=set(args, 'icon', random(random.font7)))
		div(class=css.body)
			h2(class=css.title)= set(args, 'title', random(random.title))
			div(class=css.text)
				p= set(args, 'text', random(random.text, 70))
