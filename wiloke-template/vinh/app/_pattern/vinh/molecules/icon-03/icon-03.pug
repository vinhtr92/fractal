//- demo: true
mixin vinhIcon03(args)
	include cssmodule

	+module('div', css.module, args)
		- let args = item
		
		//- code

		h3(class=css.title)= set(args, 'title', random(random.title))
		div(class=css.text)
			p= set(args,'text', random(random.text, 80))
		
	include cssmodule
