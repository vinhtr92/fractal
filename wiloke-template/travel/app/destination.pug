extends pug/layout

//- Config
block config
	-
		config.title = 'destination'
		config.bodyClass = ''

//- afterLink
block afterLink
	
//- Header
block header
	+longHeader({
	})
//- Footer
block footer
	
//- Content
block content
	+long_hero({
		img: `img/bg/4.jpg`,
		overlay: true,
		align: 'center',
		modifier: 'style02',
	})
		+vinhHerotext({
			title: 'Destinations',
			desc : 'Let me help you plan your trip of a lifetime!',
			modifier: 'style03',
		})

	+section({
		container: true,
		css: {
			'background':'#fcfcfc'
		},
		addClass: 'pt-60 pb-30',
	})
		+row
			+col({
				cols: [12]
			})
				+vinhForm({
					modifier: 'search-wide',
				})



	+section({
		container: true,
		css: {
			'background':'#fcfcfc'
		},
		addClass: 'pt-30 py-10',
	})
		+vinhSectitle({
			title: 'Filter by Region',
			desc : '+1238 most beautiful tourist destinations in the world',
			addClass: 'mb-20'
		})

		+row
			+vinhImagebox({
				addClass: 'mb-30',
				loop: {
					data: [
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`
						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`
						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`
						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`
						}
						],
					wrap: 'col-sm-12 col-md-6 col-lg-3'
				}
			})





	+section({
		container: true,
		css: {
			'background':'#fcfcfc'
		},
		addClass: 'pt-30 py-10',


	})
		+vinhSectitle({
			title: 'Filter by Country',
			desc : '+1238 most beautiful tourist destinations in the world',
			addClass: 'mb-20'
		})

		+row
			+vinhImagebox({
				addClass: 'mb-30',
				loop: {
					data: [
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						},
						{
							modifier: 'style06',
							category: random(random.continental),
							desc: `${random(1,100)} destinations`

						}],
					wrap: 'col-sm-12 col-md-6 col-lg-3'
				},
			})
	
	
	
	
	
	+section({
		container: true,
		css: {
			'background':'#fcfcfc',
		},
		addClass: 'pt-30 pb-60',

	})
	
		+vinhSectitle({
			title: 'General Travel Tips and Resources',
			desc : '+1238 most beautiful tourist destinations in the world',
			addClass: 'mb-25',
		})

		+row
			+vinhImagebox({
				loop: {
					data: [
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						},
						{
							modifier: 'style04',
							categoryColor: random(random.textcolor)
						}
						],
					wrap: 'col-sm-12 col-md-6 '
				},
			})


	+vinhFooter({

	})