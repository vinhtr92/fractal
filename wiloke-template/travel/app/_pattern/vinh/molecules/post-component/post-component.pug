//- demo: true
mixin vinhPostComponent(args)
	include cssmodule

	+module('div', css.module, args)
		- let args = item
		
		//- code
		if (args.title != null)
			h3.postComponent-title= args.title || random(random.title)
		if block
			block

	include cssmodule
