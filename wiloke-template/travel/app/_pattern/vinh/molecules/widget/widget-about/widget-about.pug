//- demo: true
mixin vinhWidgetAbout(args)
	include cssmodule

	+module('div','', args)
		- let args = item
		- let imgsrc = `img/avatar/${ random(1, 20) }.jpg`

		
		//- code
		a.widget_about__header(href='#')
			div.widget_about__avatar(style=`background-image:url('${args.imgsrc||imgsrc}')`)
				img(src= args.imgsrc || imgsrc)
			h3.widget_about__name= args.name || random(random.people.name)
			div.widget_about__desc= args.desc || random(random.portfolio.categories)

		div.widget_about__body
			div.widget_about__content= args.content || random(random.text,200)
		
	include cssmodule
