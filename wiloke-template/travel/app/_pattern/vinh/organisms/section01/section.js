function section() {
	const css = {"module":".vinh_section01__module","":""};
	const items = [...document.querySelectorAll('.vinh_box4__module')];
	// items.forEach((item) => item.clientWidth < 320 ? item.wilAddClass('small') : console.log(item));
	
}
section();
