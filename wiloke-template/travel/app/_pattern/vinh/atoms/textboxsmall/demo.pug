<div class="wil-core-space"><span></span><div class="wil-text-modifier"><span>default</span></div></div>
//- Args: id, icon, iconColor, iconClass, textClass, link, classText, text, bgColor, category
+vinhTextboxSmall({
	
})
<div class="wil-core-space"><span></span><div class="wil-text-modifier"><span>modifier: 'reverse'</span></div></div>
//- Args: id, icon, iconColor, iconClass, textClass, link, classText, text, bgColor, category
+vinhTextboxSmall({
	modifier: 'reverse'
})
<div class="wil-core-space"><span></span><div class="wil-text-modifier"><span>modifier: 'category'</span></div></div>
//- Args: id, icon, iconColor, iconClass, textClass, link, classText, text, bgColor, category
+vinhTextboxSmall({
	modifier: 'category'
})
