//- demo: true
mixin longHeading(args)
	include cssmodule

	+module('div', css.module, args)
		- let args = item
		
		h2(class=css.title)= args.title || 'this is a title'
		div(class=css.text)= args.text || random(random.text, 200)
		
		
		
	include cssmodule
