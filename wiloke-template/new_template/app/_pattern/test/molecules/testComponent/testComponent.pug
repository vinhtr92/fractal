//- demo: true
mixin testTestcomponent(args)
	include cssmodule

	+module('div', css.module, args)
		- let args = item
		
		//- code
		h2(class=css.title)= args.title || 'day la title default'
		div(class=css.text)= args.text || random(random.text, 200)
		
		
	include cssmodule
